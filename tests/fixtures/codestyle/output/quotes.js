// ❌ invalid examples that will be fixed to use single quotes
const foo = 'foo'
const baz = 'baz'
const escape = 'Escape \'the\' single quote'

// ✅ valid usage of quotes
const bar = 'bar'
const escape2 = "Escape 'the' single quote"
const template = `Usage of ${bar}`
